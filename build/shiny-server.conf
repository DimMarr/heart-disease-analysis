run_as shiny;

server {
  listen 3838;

  # Sert l'application au chemin /app pour correspondre au reverse proxy nginx
  location /app {
    app_dir /srv/shiny-server;
    log_dir /var/log/shiny-server;
  }
}
